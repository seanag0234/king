# fish shell completion for {{rootName}}
# generated by king (https://github.com/miekg/king) for kong
{{- define "command"}}
{{- if not .Parent}}
# {{rootName}}
{{- else}}
# {{path .}}
complete -c {{rootName}} -f -n '{{subcommandCondition .}}' -a {{.Name}} -d '{{.Help}}'
{{- end}}
{{- range flagLines .}}
{{.}}
{{- end}}
{{range visibleChildren .}}
{{- template "command" .}}
{{- end}}
{{- end}}
{{- template "command" .}}
