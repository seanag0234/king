# fish shell completion for {{rootName}}
# generated by king (https://github.com/miekg/king) for kong
{{- define "command"}}
{{- if not .Parent}}
# {{rootName}}
{{- else}}
# {{path .}}{{if .Aliases}} ({{join .Aliases ", "}}){{end}}
complete -c {{rootName}} -f -n '{{subcommandCondition .}}' -a {{.Name}} -d '{{escape .Help}}'
{{- range .Aliases}}
complete -c {{rootName}} -f -n '{{subcommandCondition $}}' -a {{.}} -d '{{escape $.Help}}'
{{- end}}
{{- end}}
{{- range flagLines .}}
{{.}}
{{- end}}
{{- range positionalLines .}}
{{.}}
{{- end}}
{{range visibleChildren .}}
{{- template "command" .}}
{{- end}}
{{- end}}
{{- template "command" .}}
